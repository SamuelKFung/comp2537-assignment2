<%- include("templates/header") %>
    <% if (!username) { %>
        <a href='/signup' class="btn btn-success"><button>Sign up</button></a><br>
        <a href='/login' class="btn btn-secondary"><button>Log in</button></a>
    <% } else { %>
        Hello, <%= username %>!<br>
        <a href='/members' class="btn btn-primary"><button>Go to Members Area</button></a><br>
        <a href='/logout' class="btn btn-danger"><button>Logout</button></a>
    <% } %>
<%- include("templates/footer") %>